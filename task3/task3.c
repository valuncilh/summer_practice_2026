#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv)
{
    if(argc < 4){
        fprintf(stderr, "usage: %s in.txt out.txt type_of_sort\n", argv[0]); return 1;
    }

    

    return 0;
}